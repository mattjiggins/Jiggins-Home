{
	"scripts": {
		"dev": "harp server & browser-sync start --proxy 'localhost:9000' --files 'public/**/*.ejs, public/**/*.jade, public/styles/*.scss, public/scripts/*.js'"
	},
	"devDependencies": {
		"browser-sync": "^1.3.7",
		"harp": "^0.13.0"
}
}